import 'package:flutter/material.dart';
import 'package:state_project/src/app.dart';

void main() {
  runApp(const MyApp());
}

// enum CurrentStatus { success, loading, failed, empty }
//  ValueNotifier<bool> isLoading = ValueNotifier<bool>(false);

